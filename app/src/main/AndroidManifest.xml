<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vedro401.reallifeachievement">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.SearchActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ui.FeedActivity" />
        <activity
            android:name=".ui.AddedAchievements.achievementEditor.AchievementEditorActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ui.SignInActivity" />
        <activity android:name=".ui.SignUpActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.StoryEditorActivity" />
        <activity android:name=".ui.AddedAchievements.AddedAchievementsActivity" />
        <activity android:name=".ui.AchievementActivity"></activity>
    </application>

</manifest>